import 'package:flutter/material.dart';

class srcDstQR extends StatelessWidget {
  const srcDstQR({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("src and dest stations"),
      ),
    );
  }
}
