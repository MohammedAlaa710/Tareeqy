import 'package:flutter/material.dart';

class numberOfStaionsQR extends StatelessWidget {
  const numberOfStaionsQR({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("no. stations"),
      ),
    );
  }
}
